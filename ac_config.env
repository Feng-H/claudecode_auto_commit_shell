# Claude Code 自动Commit配置文件
# Auto Commit Configuration for Claude Code

# Commit消息语言: zh-CN (中文) 或 en (英文)
# Commit message language: zh-CN (Chinese) or en (English)
COMMIT_LANGUAGE=zh-CN

# 如果不是git仓库，是否自动初始化
# Auto-initialize git repository if not already initialized
AUTO_INIT=true

# 最大分析diff行数（避免token浪费）
# Maximum diff lines to analyze (to avoid token waste)
MAX_DIFF_LINES=200

# 使用Claude API生成commit消息
# Use Claude API to generate commit messages
USE_CLAUDE_API=true

# API超时时间（秒）
# API timeout in seconds
API_TIMEOUT=30
